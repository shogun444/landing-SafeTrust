---
import ThemeToggle from "./ThemeToggle.astro";

const navLinks = [
  { label: "Features", href: "#features" },
  { label: "How It Works", href: "#how-it-works" },
  { label: "Pricing", href: "#pricing" },
];
---

<nav class="navbar">
  <a href="/" class="navbar-brand">
    <img src="/landing-logo.jpeg" alt="SafeTrust logo" width="32" height="32" />
    <span>SafeTrust</span>
  </a>

  <input type="checkbox" id="nav-toggle" class="navbar-checkbox" aria-label="Toggle navigation" />
  <label for="nav-toggle" class="navbar-toggle" aria-hidden="true">
    <span></span>
    <span></span>
    <span></span>
  </label>

  <div class="navbar-links">
    {navLinks.map((link) => (
      <a href={link.href}>{link.label}</a>
    ))}
    <ThemeToggle />
    <a href="/register" class="navbar-cta">Connect Wallet</a>
  </div>
</nav>

<style>
  .navbar {
    position: sticky;
    top: 0;
    z-index: 50;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    padding: 1rem 2rem;
    backdrop-filter: blur(16px);
    background: rgba(255, 255, 255, 0.88);
    border-bottom: 1px solid rgba(15, 23, 42, 0.08);
    box-shadow: 0 1px 12px rgba(15, 23, 42, 0.06);
    transition: background 0.25s ease, box-shadow 0.25s ease;
  }

  .navbar-brand {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-weight: 700;
    font-size: 1.25rem;
    color: inherit;
    text-decoration: none;
  }

  .navbar-brand img {
    border-radius: 8px;
  }

  .navbar-links {
    display: flex;
    align-items: center;
    gap: 1.5rem;
  }

  .navbar-links a {
    color: inherit;
    text-decoration: none;
    font-weight: 500;
    transition: color 0.2s ease, transform 0.2s ease;
  }

  .navbar-links a:hover {
    color: #2857B8;
  }

  .navbar-cta {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    background: #2857B8;
    color: #fff;
    padding: 0.75rem 1.4rem;
    border-radius: 10px;
    font-weight: 600;
    text-decoration: none;
    box-shadow: 0 10px 24px rgba(40, 87, 184, 0.12);
    transition: transform 0.2s ease, box-shadow 0.2s ease, background 0.2s ease;
  }

  .navbar-cta:hover {
    transform: translateY(-1px) scale(1.02);
    box-shadow: 0 18px 30px rgba(40, 87, 184, 0.18);
    background: #1f4cb6;
  }

  .navbar-checkbox {
    display: none;
  }

  .navbar-toggle {
    display: none;
    cursor: pointer;
    background: transparent;
    border: none;
    padding: 0.5rem;
    margin-left: auto;
  }

  .navbar-toggle span {
    display: block;
    width: 1.6rem;
    height: 2px;
    margin: 0.35rem 0;
    background: #111827;
    border-radius: 999px;
    transition: transform 0.2s ease, opacity 0.2s ease;
  }

  @media (max-width: 768px) {
    .navbar {
      padding: 1rem 1.25rem;
    }

    .navbar-toggle {
      display: inline-flex;
    }

    .navbar-links {
      position: absolute;
      inset: 100% 0 auto 0;
      padding: 1rem 1.25rem 1.25rem;
      flex-direction: column;
      align-items: stretch;
      gap: 0.75rem;
      background: rgba(255, 255, 255, 0.96);
      border-bottom-left-radius: 16px;
      border-bottom-right-radius: 16px;
      border-top: 1px solid rgba(15, 23, 42, 0.08);
      box-shadow: 0 12px 24px rgba(15, 23, 42, 0.08);
      max-height: 0;
      overflow: hidden;
      opacity: 0;
      pointer-events: none;
      transition: max-height 0.25s ease, opacity 0.25s ease;
    }

    .navbar-links a {
      padding: 0.75rem 0;
      display: block;
    }

    .navbar-cta {
      width: 100%;
    }

    .navbar-checkbox:checked + .navbar-toggle + .navbar-links {
      max-height: 420px;
      opacity: 1;
      pointer-events: auto;
    }
  }

  :global(.dark) .navbar {
    background: rgba(15, 23, 42, 0.9);
    border-bottom-color: rgba(255, 255, 255, 0.08);
  }

  :global(.dark) .navbar-links a {
    color: #e2e8f0;
  }

  :global(.dark) .navbar-links a:hover {
    color: #60a5fa;
  }

  :global(.dark) .navbar-toggle span {
    background: #e2e8f0;
  }

  @media (max-width: 768px) {
    :global(.dark) .navbar-links {
      background: rgba(15, 23, 42, 0.96);
    }
  }
</style>
